# ConvertKeylogApp v2.1 - Anti-Crash Excel Requirements

# Core Excel processing
pandas>=1.5.0
openpyxl>=3.0.0

# GUI framework
tk  # Built into Python, but listed for clarity

# System monitoring for crash protection
psutil>=5.8.0

# Optional: Performance enhancements
numpy>=1.20.0  # pandas dependency, but explicit for performance

# Development and testing
pytest>=7.0.0  # For running tests
memory-profiler>=0.60.0  # For memory profiling (optional)

# File handling
os  # Built-in
json  # Built-in  
re  # Built-in
datetime  # Built-in
threading  # Built-in
time  # Built-in
gc  # Built-in
typing  # Built-in
